name: CI-test Pipeline

on:
  workflow_dispatch

jobs:

  build:
    uses: ./.github/workflows/build-petclinic.yml
    secrets: inherit

  sonar:
    needs: build
    uses: ./.github/workflows/sonarci.yml
    secrets: inherit
